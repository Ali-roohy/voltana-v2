
# Web Push (TASK-0039) — generate a pair with:
#   cd voltana-api && go run ./cmd/genvapid   (or any webpush VAPID generator)
VAPID_PUBLIC_KEY=changeme-vapid-public
VAPID_PRIVATE_KEY=changeme-vapid-private
